#include <stdio.h>
#include <stdlib.h>

// VETOR DE LIVROS
// Guarda um ponteiro para Livros

// Livro **livros = (Livro**) calloc(3, sizeof(Livro));
// livros[0] = criarLivro("Harry Potter", 200, 25);
// livros ..
// livros ..

// destroy
// for(int i = 0; i < 3; i++) {
//      destruirLivro(&livros[i]);
// }
// free(livros);
// livros = NULL;